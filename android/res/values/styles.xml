<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="SamsungOne700C">
        <item name="android:fontFamily">@font/samsungone700c</item>
    </style>

    Нужен для прозрачного статус бара
    <style name="AstroTheme" parent="Base.Theme.AstroAxis">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:statusBarColor">#293133</item> <!-- Этот цвет будет определять степень затемнения -->
    </style>

    Кастомизация диалогово окна выхода из приложения
    Используется в классе MainMenuActivity в методе exitApplication()
    <style name="AlertDialogCustom" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/dialog_background</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
        <item name="android:fontFamily">@font/samsungone800c</item>
    </style>

    Кастомизация диалогово окна статус бара загрузки
    Используется для загрузки активности фреймворка LibGDX в классе PlanetActivity в методе openModelButton(String namePlanet)
    <style name="CustomProgressDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:indeterminateTint">@color/dark_blue</item> <!-- Здесь указывается цвет спиннера -->
        <item name="android:windowBackground">@drawable/dialog_background</item> <!-- Здесь указывается ваш кастомный фон -->
        <item name="android:textColor">@color/white</item> <!-- Здесь указывается цвет текста -->
        <item name="android:textSize">18sp</item>
        <item name="android:fontFamily">@font/samsungone800c</item>
    </style>

    Кастомизация диалогово окна c изображением
    Используется в классе TheoryActivity в методе fullScreenImage(ImageView nameImage, int drawableIdImage)
    <style name="FullScreenDialogStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>

</resources>
